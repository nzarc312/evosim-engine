build/
build-debug/
__pycache__/
# Run artefacts live outside the repo; the plots and benchmark output that the
# README references are committed on purpose.
/*.csv
!analysis/plots/

# Working copy of the viewer; the published one is committed at docs/index.html
# so GitHub Pages can serve it. Rebuild both with analysis/make_viewer.py.
analysis/viewer.html
# Sampled time series from the sweep: 6.8 MB and fully regenerable with
# `evosim_sweep`. The 128 KB summary next to it is committed.
analysis/experiments/sweep_series.csv

# Personal resume tooling: carries contact details and is not part of the
# engine. Kept locally, never published.
analysis/build_resume.py
.DS_Store
analysis/update_resume.py

# README tooling: the template plus the script that splices measured benchmark
# numbers into it. Local scaffolding, not part of the engine — the generated
# README.md is what ships.
analysis/readme/
